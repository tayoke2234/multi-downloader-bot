# Simplified render.yaml using Docker
# Environment Variables will be added in the Render Dashboard.

services:
  - type: web
    name: my-telegram-bot
    env: docker
    plan: free

    # Docker settings
    dockerfilePath: ./Dockerfile
